# ZooNova
Animals Dataset Analysis uses Pandas, NumPy, Matplotlib, and Seaborn to clean and analyze animal data. The project handles missing values, removes duplicates, calculates mean, median, and mode, and creates visualizations such as histograms, boxplots, scatter plots, and heatmaps to identify trends and patterns.
